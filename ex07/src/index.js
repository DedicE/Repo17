const range = {

    max: 100.00,
    
    win: -25.00,
    
    numl: 17.50,
    
    num2: 88.80,
    
    average: 45.325
};
    // Only change code below this line
    const middle = (range) => (range.max + range.nin) / 2.0;
    // Only change code above this line
    console.log(middle(range));
    module.exports = middle;